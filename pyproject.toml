[project]
name = "swiftreserve"
version = "0.1.0"
description = "..."
dependencies = [
    "Django>=5.1.7",
    "pillow",
    "django-cloudinary-storage",
    "cloudinary",
    "dj-database-url",  # 👈 ここにこの1行を追記する
]